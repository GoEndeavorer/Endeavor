export const metadata = {
  title: "Admin | Endeavor",
  description: "Platform administration and moderation tools.",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
