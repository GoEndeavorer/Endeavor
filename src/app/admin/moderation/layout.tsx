export const metadata = {
  title: "Moderation | Admin | Endeavor",
  description: "Review and manage reported content across the platform.",
};

export default function ModerationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
